rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if false;
      allow delete: if true;  // Allow delete operations for clearing data
    }
  }
}
